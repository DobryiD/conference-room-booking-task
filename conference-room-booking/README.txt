Building and deploying the application

For building required:
apache-maven-3.6.2,
java jdk 1.8 or higher.


For building you have to  run this "mvn install" command from the application folder which contains the pom.xml file.
Then to deploy the application you can use "mvn spring-boot:run" command.
To exit you might use short cut CTRL+C.
